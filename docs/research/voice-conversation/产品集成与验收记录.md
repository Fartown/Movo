# 连续语音对话产品集成与验收记录

更新：2026-09-22。实现基线为 main `75152a7`，开发分支 `fix/voice-conversation`。本文记录产品代码和实测结果，不将此前独立 SDK 探针结果计为产品通过。

## 当前接法

采用已验证的 Dialog 客户端委托播报模式。`DoubaoDialogEngine` 使用官方 SDK 0.0.15.0 持有录音、AEC 与播放器；`VoiceConversationController` 管理会话，`VoiceTurnCoordinator` 管理用户输入、任务与播报的归属。现有 `EtaAssistantOverlayService` 增加 microphone 前台服务类型，页面显示和隐藏不销毁对话。进入对话前暂停 Sherpa 本地唤醒，SDK 真正销毁后恢复唤醒，禁止两个录音器抢麦。

`ASR_INFO` 选择客户端播报，`ASR_RESPONSE` 更新字幕，`ASR_ENDED` 后留 650 ms 补话窗口，再自动提交一次。窗口内再起声则合并未提交输入。R9 真机发现 PCM 已恢复、云 ASR_INFO 尚未到达时仍可能误提交；R10 增加本地 PCM 活动否决，等待下一 ASR 段合并。活动未获识别确认超过 6 s 则保留草稿并结束连接，禁止强行发半句话。无需手动结束录音或点击发送。当前输入端点参数为 custom VAD、1000 ms end smooth window，准确性以真实语料验证为准。

语音通过 `AgentAppState.sendVoiceMessage` 进入文字聊天同一 write-ahead、角色准备、模型配置、Runtime、历史与结果确认流程。保留当前 DeepSeek、推理强度和工具设置。语音风格作为公共 PromptBuilder 中的一条会话级系统消息，普通与角色会话均生效，不修改全局配置或历史正文。Runtime 增加语音参与时的原子准入、runId 持久化去重，以及 rejected/unconfirmed 的来源区分；断连不自动重发可能已执行的任务。

按用户明确要求，只播报本会话仍有输出权限的 LLM 最终正文，不播报思考、工具、执行日志或系统状态。失败和取消只显示在界面；R10 删除原有错误/取消状态的播报旁路。Dialog 的 `chat_tts_text`、question_id、reply_id 用于归属；默认豆包回答不进入 Movo 历史或播放器。每轮客户端选择并不关闭云端默认生成，费用不能按“只用了 ASR/TTS”估算。

播放器在收到匹配回复的合成结束、完整时长已流逝且实时输出静音尾段满足条件后结束播报；独立 TTS 的 3020 完成事件在 Dialog 探针中未出现，不能依赖该事件。起声暂停、有效转写丢弃旧声音，任务继续；只有明确取消命令才请求取消 run。“结束对话”关闭声音通道，已接受的任务继续保存文字结果，未提交的输入转草稿。

## 实施与原方案的关系

原方案中的自建共享采集、独立 ASR/TTS 会话及句槽设计是选择 Dialog 前的基线。Dialog 接法复用 SDK 录放和服务端轮次，没有再建设第二套自动降级链路。任务与声音分离、去重、未知状态对账、共享历史等产品契约保持。

以下门槛不能因换 SDK 而删除：唤醒连说是否丢字、中文句中停顿、真实外放可懂度、双讲保真、精确停声、后台和焦点中断、播放事实持久化。本文分别记录，不能将“设置了 AEC”当双讲通过，不能将合成 PCM 当用户实际听见。

## 当前证据

| 验证 | 结果 | 证据和边界 |
|---|---|---|
| 语音/Runtime 定向单元回归 | PASS，112 项、22 suites | `tmp/tasks/2026-09-22-voice-conversation-implementation/unit-9-r14.log`，R14 源码复核；不替代设备验收 |
| Release R3 构建、签名和 16 KB 对齐 | PASS | 3.0.7 / 2026092207；APK SHA256 `745bd7ed25573602436e4fff0ac49b0c2748653b449b6715db19c6fbc4e14939`，沿用现有测试签名 |
| 正式 App 首轮自动提交 | 已观察成功 | 小米 15 / Android 15；11.36 s ASR_ENDED，12.015 s 自动 dispatch，15.294 s DeepSeek 回答 |
| 正式 App 首轮播报 | FAIL | 15.366 s 指令执行错误结束；播放器 550080 bytes 全零，无 speaking，保留 r1 失败 |
| R3 / R6 回归 | 测试阻断 | 分别遭遇 R8 移除 Companion 与目标 Kotlin 标准库方法，语音请求未执行；保留 automation BLOCKED，不判产品通过或产品回归失败 |
| R7 回归 | INCONCLUSIVE | 首轮完整走完 speaking → listening，播放器 995520 bytes、190865 个非零样本。原 runner 因“我记着了”不含“记住”错误中断，原 FAIL 文件保留；未完成后续轮次，不判整体通过 |
| R9 / v3 回归 | FAIL | 首轮真实自动提交、正文和播报成功；第二轮 1.2 s 句中停顿后半句被提前提交。ASR_INFO 比错误 dispatch 晚 303 ms，最终两个样本产生三个 dispatch。保留截图、153.04 s 录屏和原事件 |
| R10 / v4 回归 | FAIL | 补话否决生效：31.819 s endpoint、31.971 s PCM 活动、32.837 s 新 ASR 段、37.476 s 合并后只 dispatch 一次。4 条 speech.request 与正文逐字相同。插话替换 turn5 有正文但无声音，20 s 后播放超时；语音结束未执行。108.04 s 录屏和全部事件已保留 |
| R11 / v4 回归 | FAIL | 替换回答恢复出声，但 PCM 切段转录为“成分，蓝精四十七”，新正文只有“蓝鲸47”，证实有旧声音残留。新轮 audible 比匹配的 client TTS 开始早 155 ms，不能用 turn 标签证明清队列。另外结束路径漏发 ended，监控读取半个 UTF-8 字符导致录像中断；全部失败事实保留 |
| R12 构建 | 未安装 | 只修 ended 事件；发现 R11 残留声音后未继续安装，直接制作 R13 |
| R13 / v4 回归 | INCONCLUSIVE | 完整 runner PASS、4 次 dispatch，自动补话/多轮/正文来源/结束负例通过；实际替换段转录为“嗯，蓝鲸四十七”，多出的短音节尚不能排除残留。A3 保留 INCONCLUSIVE，不把标签与转录猜测当完全清队列证据 |
| R14 / v4 回归 | FAIL | 当前客户端 decoder PCM 到达才恢复播放器；替换轮上游原始正文为“鲸 47”，暗号不完整，原断言失败。首个 delta、done、终态与 speech.request 一致；没有新轮 PCM，因此 A3/A4 未完成。正常第二答的 68160 bytes decoder PCM 在 player 中逐字节找到，证明对照方法可用 |
| R14b / v4 回归 | FAIL | 同包复测，第二轮上游正文再次为“鲸 47”，尚未到达插话。原预期与失败不变；增设仅测试包诊断模式，在不完整正文时继续采集播放和结束证据，所有暗号问题仍在结尾判 FAIL |
| 连续多轮、补话、打断、结束、取消 | 尚未完成产品验收 | 已冻结真实服务、真实选中模型的正式产品用例；失败不覆盖 |
| 物理麦和 AEC 双讲 | 尚未验收 | 合成 PCM 输入只替换声源，不伪造 ASR 事件，但仍不等于独立近端物理人声 |

设备保留，不自动释放。测试执行遵守项目 cloud-device-test 与 test-workflow；每轮保留 artifact hash、输入来源、模型协议、原音量、状态截图、录屏及原始事件。正式报告由 run 事实生成。

R7 单样本时延：语音样本有效发声末端到 ASR_ENDED 约 1.081 s，到自动 dispatch 约 1.735 s，到 SDK 首个非零播放回调约 5.794 s；DeepSeek 仍启用原思考设置。此为合成 PCM 时轴和软件回调的单次观测，不是物理首声或 P50/P95。模型增量、done、终态和解析正文均为 42 字，本轮没有前缀丢失。

R9/R10 发现正文开头有缺主语等不自然内容。独立 HTTP SSE 探针直接请求当前 `deepseek-v4-1-flash-260910` Responses 端点，最小测试 prompt 同样得到以“太阳系中距”开头的回复：初始 message/content_part 均空，增量、done 与终态全文一致，未经过 Android 解析或 TTS。证据在 `tmp/tasks/2026-09-22-voice-conversation-implementation/model-prefix-probe/`。这是上游原始正文现象；不从思考内容拼补，也不偷偷切换用户模型或推理配置。

远端构建：[GitHub Actions 35739129371](https://github.com/Fartown/Movo/actions/runs/35739129371) 在 `dc97e09`（R13）通过 Debug/Release 构建和 APK 校验。沿用已安装 3.0.6 与本地测试包的签名，补齐原先为空的四个 Actions 发布签名 secrets；没有提交私钥。R14 仍须对最终代码做远端构建复核。
